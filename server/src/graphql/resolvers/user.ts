// import { IResolvers } from "apollo-server-express";

// export const userResolver: IResolvers = {
//   Query: {
//     user: async (_root: undefined, { id }: {id: userArgs},  { db }: { db: Database }) => {
//       return await db.user.find({}).toArray()
//     }
//   },
// }

export {}